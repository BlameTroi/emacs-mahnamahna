;;; idohow.el --- focused search helpers  -*- lexical-binding: t -*-

;;; Commentary:

;; are there faster ways to read stack overflow?

;;; Code:


;; (use-package sx
;;   :pin melpa
;;   :defer
;;   :custom
;;   (sx-question-mode-content-face ((t nil)))
;;   :config
;;   (bind-keys :prefix "C-c s"
;;              :prefix-map my-sx-map
;;              :prefix-docstring "Global keymap for SX."
;;              ("q" . sx-tab-all-questions)
;;              ("i" . sx-inbox)
;;              ("o" . sx-open-link)
;;              ("u" . sx-tab-unanswered-my-tags)
;;              ("a" . sx-ask)
;;              ("s" . sx-search)))


(provide 'idohow)
;;; idohow.el ends here
